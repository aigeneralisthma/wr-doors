/**
 * @deprecated Compatibility shim. Moved to `@/lib/media/image-helpers`
 * when the project migrated from Supabase to Neon (image URLs are now
 * local `/uploads/...` paths, not Supabase Storage URLs).
 */
export * from "@/lib/media/image-helpers";
