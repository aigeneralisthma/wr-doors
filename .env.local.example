# =============================================================================
# WR Doors × DODA — Local environment variables
# =============================================================================
# Copy this file to .env.local and fill in real values.
# .env.local is in .gitignore — never commit secrets.
# =============================================================================

# Public site URL (used in metadata, hreflang, OG tags, sitemaps)
# Local dev: http://localhost:3000
# Production: https://wrdoors.com
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# ----- Neon Postgres -----------------------------------------------------------
# From the Neon dashboard → Connection Details.
#   DATABASE_URL          → the POOLED string (host contains "-pooler"). App runtime.
#   DATABASE_URL_UNPOOLED → the DIRECT string. Used by drizzle-kit + scripts.
DATABASE_URL=""
DATABASE_URL_UNPOOLED=""

# ----- Auth.js (admin login) -------------------------------------------------
# AUTH_SECRET: generate with `openssl rand -base64 33` (or `npx auth secret`)
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"
# The admin account lives in the `admin_users` DB table. Create/reset it with:
#   ADMIN_EMAIL=you@example.com ADMIN_PASSWORD='...' pnpm admin:create
# These two vars are only read by that script — safe to leave blank here and
# pass them inline when you run it.
ADMIN_EMAIL=""
ADMIN_PASSWORD=""

# ----- Image uploads (local filesystem) --------------------------------------
# Admin uploads are written to public/uploads/ and served at /uploads/.
# Defaults are fine for local dev and Hostinger — override only if the
# uploads folder lives outside the app dir (e.g. a mounted volume).
# UPLOADS_DIR="/var/www/wr-doors-uploads"
# NEXT_PUBLIC_UPLOADS_BASE_URL="/uploads"

# ----- Resend (email) --------------------------------------------------------
RESEND_API_KEY=""
# "From" address for transactional emails (must be on a verified Resend domain)
RESEND_FROM_EMAIL="WR Doors <noreply@wrdoors.com>"
# Where admin notifications go
ADMIN_NOTIFICATION_EMAIL="wahatalruman36@gmail.com"

# ----- Optional integrations ------------------------------------------------
# Spline scene URL (used in homepage hero)
NEXT_PUBLIC_SPLINE_SCENE_URL=""
